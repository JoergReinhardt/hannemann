<li>
  <%= gravatar_for user %>
  <%= link_to  user.name, user_path(user.login) %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "löschen", user, method: :delete, confirm: "wirklich löschen?", id: "löschen" %>
  <% end %>
</li>
